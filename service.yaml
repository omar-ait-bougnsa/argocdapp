apiVersion: v1
kind: Service
metadata:
  name: playground
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: playground
  ports:
    - port: 8888
      targetPort: 8888
      protocol: TCP
